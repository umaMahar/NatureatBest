<table>
  
    <%= form_for(@product) do |f| %>
      <% if @product.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

          <ul>
          <% @product.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %> 
    <tr class = "<%= cycle('list_line_odd','list_line_even')%>">
      <td><%= f.label :prod_name %></td>
      <td><%= f.text_field :prod_name %></td>
    </tr>
    <tr class = "<%= cycle('list_line_odd','list_line_even')%>">
      <td><%= f.label :prod_desc %></td>
      <td><%= f.text_area :prod_desc %></td>
    </tr>
    <tr class = "<%= cycle('list_line_odd','list_line_even')%>">
      <td><%= f.label :prod_price %></td>
      <td><%= f.text_field :prod_price %></td>
    </tr>
    <tr class = "<%= cycle('list_line_odd','list_line_even')%>">
      <td><%= f.label :prod_img %></td>
      <td><%= f.text_field :prod_img %></td>
    </tr>
    <tr class = "<%= cycle('list_line_odd','list_line_even')%>">
      <td><%= f.label :ProdType %></td>
      <td><%= f.text_field :ProdType %></td>
    </tr>
    <tr >
      <td><%= f.submit %></td>
    </tr>
    <% end %>

</table>
